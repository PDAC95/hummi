---
phase: 01-foundation-long-lead-vendors
plan: 07
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04", "01-05", "01-06"]
files_modified:
  - src/__red-test-marker.ts
  - .planning/phases/01-foundation-long-lead-vendors/RED-TEST-EVIDENCE.md
  - .planning/phases/01-foundation-long-lead-vendors/SENTRY-EVIDENCE.md
  - .planning/phases/01-foundation-long-lead-vendors/PHASE-1-VERIFICATION.md
autonomous: false
requirements: [INFRA-04, INFRA-09, PLAT-07, SEC-07]

must_haves:
  truths:
    - "A PR exists from test/secret-leak-red → main with an intentional service_role string in src/, and CI fails on that PR"
    - "The test/secret-leak-red branch has branch protection (cannot be deleted or force-pushed)"
    - "The supabase/functions/_synthetic-error/ Edge Function is deployed to staging and SENTRY_DSN is set"
    - "Invoking the Edge Function produces a 500 + 'logged' response, and the error appears in Sentry dashboard within 30s"
    - "A synthetic browser-side error (test page or browser console throw) appears in Sentry dashboard"
    - "Phase 1 verification doc enumerates all 5 success criteria from ROADMAP.md as PASS or describes the gap"
  artifacts:
    - path: "src/__red-test-marker.ts"
      provides: "Intentional service_role leak — only exists on test/secret-leak-red branch; NEVER in main"
      contains: "SUPABASE_SERVICE_ROLE_KEY"
    - path: ".planning/phases/01-foundation-long-lead-vendors/RED-TEST-EVIDENCE.md"
      provides: "Documents the red-test PR URL + CI failure screenshot, proving guardrail works"
      contains: "PR URL, secret-leak-grep job failure link"
    - path: ".planning/phases/01-foundation-long-lead-vendors/SENTRY-EVIDENCE.md"
      provides: "Documents the synthetic Sentry events (SPA + Edge) with dashboard screenshots"
      contains: "Sentry event IDs for SPA and Edge captures"
    - path: ".planning/phases/01-foundation-long-lead-vendors/PHASE-1-VERIFICATION.md"
      provides: "Phase-level verification — all 5 ROADMAP success criteria checked off"
      contains: "5-row table mapping success criterion → evidence"
  key_links:
    - from: "test/secret-leak-red branch"
      to: "PR against main"
      via: "CI runs the 5 PR checks; secret-leak-grep job FAILS"
      pattern: "secret-leak-grep.*FAIL"
    - from: "supabase/functions/_synthetic-error"
      to: "Sentry hummi-edge dashboard"
      via: "deployed + invoked + flush → event arrives within 30s"
      pattern: "sentry-synthetic-edge-function-error"
---

<objective>
Prove that everything built in Wave 1 and Wave 2 actually works. This is the phase-level verification plan — the goal is producing evidence (PR URLs, screenshots, dashboard event IDs) for each of the 5 ROADMAP success criteria.

Purpose: Without this plan, Phase 1 is "we built the workflows" — with it, Phase 1 is "the workflows actively block bad commits and capture errors." Without proof, future regressions go undetected.
Output: Red-test branch + PR (living guardrail proof), synthetic Sentry events captured, phase verification doc enumerating all 5 success criteria.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-long-lead-vendors/01-CONTEXT.md
@.planning/phases/01-foundation-long-lead-vendors/01-RESEARCH.md
@.planning/ROADMAP.md

Reference research sections:
- §"Code Examples → Red-Test Setup Procedure" — exact commands for the red-test branch
- §"Code Examples → Gitleaks GitHub Actions Workflow" — already in place from Plan 03
- §"Validation Architecture" — per-requirement validation map

Read credentials from (gitignored):
@.planning/phases/01-foundation-long-lead-vendors/account-credentials.md
(STAGING_PROJECT_ID, SENTRY_DSN_EDGE, SUPABASE_ANON_KEY)

Phase 1 ROADMAP.md success criteria (must all be verified here):
1. A developer can `supabase db push` a migration against staging and a CI step blocks merges that would break the prod schema (verified by Plan 02 + 03; this plan re-confirms)
2. CI fails any PR that introduces `VITE_.*SERVICE` or `service_role` strings inside `src/` (verified by intentional red test) ← THIS PLAN'S MAIN DELIVERABLE
3. `npm audit` reports zero high/critical findings; gitleaks history scan has been run once and any exposed keys rotated (verified by Plan 05; re-confirmed)
4. Twilio toll-free CSCA verification, Stripe Canadian bank verification, Resend SPF/DKIM/DMARC warmup, and CRA HST registration are all in flight with documented start dates (verified by Plan 06; this plan checks VENDORS.md)
5. Sentry captures a synthetic error from both the SPA and a sample Edge Function, viewable in the dashboard ← THIS PLAN'S MAIN DELIVERABLE
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create red-test branch + PR proving CI blocks merge</name>
  <files>src/__red-test-marker.ts, .planning/phases/01-foundation-long-lead-vendors/RED-TEST-EVIDENCE.md</files>
  <action>
This task creates a permanent piece of CI red-test evidence per RESEARCH.md §"Red-Test Setup Procedure". The branch and PR stay alive forever as living proof that the secret-grep blocks merges.

1. **Verify you are on a clean main branch:**
   ```powershell
   git status     # must be clean
   git checkout main
   git pull
   ```

2. **Create the red-test branch and the offending file:**
   ```powershell
   git checkout -b test/secret-leak-red
   ```

   Create `src/__red-test-marker.ts` with exactly this content (note: pre-commit hook from Plan 05 WILL block this — we use `--no-verify` ONLY for this one commit, since the whole point is testing the CI catch):
   ```typescript
   // src/__red-test-marker.ts
   // DO NOT MERGE — this file exists ONLY on branch test/secret-leak-red to verify
   // the CI secret-leak-grep guardrail (INFRA-04 / SEC-07).
   //
   // If you see this file on `main`, the guardrail is BROKEN.
   const SUPABASE_SERVICE_ROLE_KEY = "fake.eyJSAMPLEKEY.shouldNeverShipToProd";
   export { SUPABASE_SERVICE_ROLE_KEY };
   ```

3. **Commit and push, bypassing the local pre-commit hook intentionally:**
   ```powershell
   git add src/__red-test-marker.ts
   git commit --no-verify -m "test: red-test marker for secret-leak guardrail (DO NOT MERGE)"
   git push origin test/secret-leak-red
   ```

   The pre-commit hook from Plan 05 will try to block this — `--no-verify` is the one-time bypass. The CI catch is what we're testing here, not the local hook (which was already verified in Plan 05's checkpoint).

4. **Open a PR from `test/secret-leak-red` → `main`** using `gh` CLI:
   ```powershell
   gh pr create --base main --head test/secret-leak-red `
     --title "test: red-test marker for secret-leak guardrail (DO NOT MERGE)" `
     --body "This PR exists as living proof that the CI 'Secret Leak Grep' job blocks merges containing service_role / VITE_.*SERVICE patterns. It MUST NEVER be merged. See .planning/phases/01-foundation-long-lead-vendors/RED-TEST-EVIDENCE.md."
   ```

   Capture the PR URL output.

5. **Wait for CI to run** (~2-3 minutes). Then verify:
   ```powershell
   gh pr checks {PR_URL}
   ```

   Expected: the `Secret Leak Grep (INFRA-04 / SEC-07)` check shows FAIL. Other checks (lint, tsc, build, supabase dry-run) likely pass because the file compiles and adds no migration. That's fine — we only care about the secret-grep catch.

6. **Write evidence file** `.planning/phases/01-foundation-long-lead-vendors/RED-TEST-EVIDENCE.md`:
   ```markdown
   # Red-Test Evidence — Secret Leak Grep (INFRA-04 / SEC-07)

   **Goal:** Prove the CI secret-leak-grep job blocks merges that introduce `service_role` / `VITE_.*SERVICE` in `src/`.

   ## The branch

   - Branch: `test/secret-leak-red`
   - File added: `src/__red-test-marker.ts`
   - Content: contains the literal string `SUPABASE_SERVICE_ROLE_KEY` and a fake JWT value

   ## The PR

   - PR URL: {PR_URL}
   - Status: **Open forever — DO NOT MERGE**
   - CI run: {CI_RUN_URL}
   - `Secret Leak Grep (INFRA-04 / SEC-07)` job: **FAILED** ✓ (this is the desired outcome)

   ## What the CI failure log shows

   ```
   ERROR: Possible secret leak detected in src/.
   Forbidden patterns: VITE_[A-Z_]*SERVICE service_role SUPABASE_SERVICE SUPABASE_SERVICE_ROLE_KEY
   ```

   ## Branch protection

   - Branch ruleset: `test/secret-leak-red` cannot be deleted or force-pushed (configured in Plan 03's `branch-protection-setup.md`)
   - This makes the red-test evidence permanent and tamper-resistant.

   ## Why we don't merge

   If we merged, the offending file would land in `main`. Future regressions could "fix" the secret-grep without anyone noticing because no PR currently fails. By keeping the PR open, we have a continuously-failing red test.

   ## Re-running

   To re-verify after any change to `scripts/secret-leak-grep.sh` or `.github/workflows/pr-checks.yml`:
   1. Go to the PR
   2. Click "Re-run all jobs"
   3. Confirm `Secret Leak Grep` still FAILs
   ```

7. **Apply branch protection to `test/secret-leak-red`** — founder action in the checkpoint (Task 3).

8. **Switch back to main:**
   ```powershell
   git checkout main
   ```
  </action>
  <verify>
    <automated>powershell -Command "gh pr list --head test/secret-leak-red --base main --state open --json url | Select-String 'http'; Test-Path .planning/phases/01-foundation-long-lead-vendors/RED-TEST-EVIDENCE.md"</automated>
  </verify>
  <done>PR exists from test/secret-leak-red → main; PR is open; the Secret Leak Grep CI job has failed (verifiable via gh pr checks); RED-TEST-EVIDENCE.md is written with the PR URL.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy synthetic-error Edge Function + capture Sentry events</name>
  <files>.planning/phases/01-foundation-long-lead-vendors/SENTRY-EVIDENCE.md</files>
  <action>
1. **Read credentials** from `.planning/phases/01-foundation-long-lead-vendors/account-credentials.md` — extract STAGING_PROJECT_ID, SENTRY_DSN_EDGE, plus the staging anon key from Supabase dashboard.

2. **Set the SENTRY_DSN secret on staging Supabase:**
   ```powershell
   supabase secrets set SENTRY_DSN="<SENTRY_DSN_EDGE value>" --project-ref <STAGING_PROJECT_ID>
   supabase secrets set SUPABASE_ENV="staging" --project-ref <STAGING_PROJECT_ID>
   ```

3. **Deploy the synthetic-error function:**
   ```powershell
   supabase functions deploy _synthetic-error --project-ref <STAGING_PROJECT_ID>
   ```

   If deploy fails (e.g., npm specifier resolution issue), check the function logs:
   ```powershell
   supabase functions list --project-ref <STAGING_PROJECT_ID>
   ```

4. **Invoke the function:**
   ```powershell
   $STAGING_URL = "https://<STAGING_PROJECT_ID>.supabase.co/functions/v1/_synthetic-error"
   $ANON_KEY = "<staging anon key from Supabase dashboard>"
   curl.exe -X POST $STAGING_URL -H "Authorization: Bearer $ANON_KEY"
   ```

   Expected: HTTP 500 response, body `logged`.

5. **Wait ~30 seconds**, then check Sentry dashboard at https://sentry.io/organizations/<SENTRY_ORG>/issues/?project=hummi-edge :
   - There should be a new event with title `sentry-synthetic-edge-function-error`
   - Capture the event ID (URL ends with `?id=...`)
   - Take a screenshot of the event page → save as `.planning/phases/01-foundation-long-lead-vendors/sentry-edge-event.png`

6. **Trigger a synthetic SPA error.** Two options:

   **Option A — temporary dev route (cleaner):** Add a `<button onClick={() => { throw new Error('sentry-synthetic-spa-error') }}>` to any dev-only page in the SPA, run `npm run dev`, click it.

   **Option B — browser console (faster, no code change):** Run `npm run dev`, open the running app at http://localhost:5173, open browser DevTools console, paste:
   ```javascript
   setTimeout(() => { throw new Error("sentry-synthetic-spa-error") }, 0);
   ```
   The `setTimeout` ensures the throw escapes the console eval and hits the global error handler (where Sentry catches it).

   Pick Option B for speed (no permanent code in src/).

7. **Wait ~30 seconds**, then check Sentry → hummi-spa project for the event:
   - Title: `sentry-synthetic-spa-error`
   - Capture event ID, screenshot → `.planning/phases/01-foundation-long-lead-vendors/sentry-spa-event.png`

8. **Write evidence file** `.planning/phases/01-foundation-long-lead-vendors/SENTRY-EVIDENCE.md`:
   ```markdown
   # Sentry Evidence — INFRA-09 / PLAT-07

   **Goal:** Prove that Sentry captures unhandled errors from both the SPA (browser) and Edge Functions.

   ## SPA event

   - Sentry project: `hummi-spa`
   - Event title: `sentry-synthetic-spa-error`
   - Event ID: {event_id_spa}
   - URL: {sentry_url_spa}
   - Screenshot: `.planning/phases/01-foundation-long-lead-vendors/sentry-spa-event.png`
   - Method: thrown via setTimeout from browser console while running `npm run dev`

   ## Edge Function event

   - Sentry project: `hummi-edge`
   - Event title: `sentry-synthetic-edge-function-error`
   - Event ID: {event_id_edge}
   - URL: {sentry_url_edge}
   - Screenshot: `.planning/phases/01-foundation-long-lead-vendors/sentry-edge-event.png`
   - Method: `curl POST /functions/v1/_synthetic-error` on hummi-staging

   ## Conclusion

   INFRA-09 + PLAT-07 satisfied: errors from both runtimes are visible in Sentry dashboards within ~30s of occurrence.

   ## What to do if events don't appear

   - SPA: check `VITE_SENTRY_DSN` is set in `.env.local`; check Network tab in DevTools for outbound requests to `sentry.io`
   - Edge: check `supabase secrets list --project-ref <STAGING_PROJECT_ID>` confirms SENTRY_DSN is set; check function logs with `supabase functions logs _synthetic-error --project-ref <STAGING_PROJECT_ID>` for `await Sentry.flush(2000)` errors
   ```
  </action>
  <verify>
    <automated>powershell -Command "Test-Path .planning/phases/01-foundation-long-lead-vendors/SENTRY-EVIDENCE.md; Test-Path .planning/phases/01-foundation-long-lead-vendors/sentry-edge-event.png; Test-Path .planning/phases/01-foundation-long-lead-vendors/sentry-spa-event.png"</automated>
  </verify>
  <done>Both Sentry events captured (event IDs + screenshots); SENTRY-EVIDENCE.md documents both with URLs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 1: Founder verifies red-test PR is failing, Sentry events visible, VENDORS.md current; applies final branch protection to test/secret-leak-red</name>
  <action>Claude has created the red-test PR and triggered both Sentry synthetic events. Founder opens the PR URL to confirm the Secret Leak Grep check is failing (red X), applies the test/secret-leak-red branch ruleset (restrict deletion + force-push), opens Sentry dashboard to confirm both  and  events appear, and cross-checks VENDORS.md has all 4 vendors with Started dates. After approving, Task 3 writes the final PHASE-1-VERIFICATION.md tying all 5 success criteria to evidence.</action>
  <what-built>Red-test PR is open and FAILING the secret-grep check; both Sentry synthetic events are visible in dashboard; VENDORS.md is current. Founder verifies and applies final branch protection.</what-built>
  <how-to-verify>
**1. Verify the red-test PR:**
   - Open the PR URL from RED-TEST-EVIDENCE.md
   - Confirm "Secret Leak Grep (INFRA-04 / SEC-07)" check shows ❌ (red X)
   - Confirm the PR cannot be merged (button disabled or grayed)

**2. Apply branch protection to `test/secret-leak-red`:**
   - GitHub repo settings → Branches → Add ruleset for `test/secret-leak-red`
   - Enable: Restrict deletions, Restrict force pushes, Require PR (with 99 required reviews — making it effectively unmergeable)
   - Save

**3. Verify Sentry SPA event:**
   - Open https://sentry.io/organizations/<SENTRY_ORG>/issues/?project=hummi-spa
   - Confirm "sentry-synthetic-spa-error" appears as a recent issue
   - Click into it; verify stack trace is readable (source maps applied)

**4. Verify Sentry Edge event:**
   - Same URL with `?project=hummi-edge`
   - Confirm "sentry-synthetic-edge-function-error" appears

**5. Cross-check VENDORS.md (from Plan 06):**
   - Open `.planning/VENDORS.md`
   - Confirm all 4 vendor rows have Started dates + Status != "Not started"
   - Confirm evidence screenshots exist under `.planning/vendors/{vendor}/`

**6. Write the final phase verification doc** (Task 4 below will scaffold; founder reviews + confirms).
  </how-to-verify>
  <resume-signal>Type "approved" once: (a) red-test PR is open + failing, (b) branch protection applied to test/secret-leak-red, (c) both Sentry events are visible, (d) VENDORS.md is current with all 4 vendors. If anything is missing, describe what.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write phase-level verification doc</name>
  <files>.planning/phases/01-foundation-long-lead-vendors/PHASE-1-VERIFICATION.md</files>
  <action>
After the checkpoint passes, write `.planning/phases/01-foundation-long-lead-vendors/PHASE-1-VERIFICATION.md` that ties everything together:

```markdown
# Phase 1 Verification

**Date:** {today}
**Status:** READY TO CLOSE / GAPS REMAINING (set based on results)

## ROADMAP.md success criteria

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | Developer can `supabase db push` against staging; CI step blocks merges that would break prod schema | PASS | Plan 02 SUMMARY (migration applied to staging); Plan 03 SUMMARY (pr-checks.yml includes supabase-dry-run job; deploy-prod.yml applies on main); branch protection requires the check |
| 2 | CI fails any PR introducing `VITE_.*SERVICE` or `service_role` inside `src/` (red test) | PASS | RED-TEST-EVIDENCE.md — PR open at {PR_URL}, "Secret Leak Grep" check failing |
| 3 | `npm audit` reports zero high/critical; gitleaks history scan run; exposed keys rotated | PASS | Plan 05 SUMMARY; gitleaks-history-scan.json archived; secrets-hygiene-runbook.md "Rotated Keys" log {N rotations / 0 rotations} |
| 4 | Twilio toll-free, Stripe Canadian bank, Resend SPF/DKIM/DMARC, CRA HST all in flight with start dates | PASS | VENDORS.md — all 4 rows with Started dates and Status != "Not started" |
| 5 | Sentry captures synthetic error from both SPA and Edge Function | PASS | SENTRY-EVIDENCE.md — event IDs {spa_id}, {edge_id}; screenshots archived |

## Requirements coverage

All 15 phase requirements addressed:

| Req | Plan | Status |
|-----|------|--------|
| INFRA-01 | 01-01 | ✓ Two Supabase projects created |
| INFRA-02 | 01-02 | ✓ Migration runbook + CLI flow established |
| INFRA-03 | 01-03 | ✓ pr-checks.yml with 5 checks |
| INFRA-04 | 01-03 + 01-07 | ✓ secret-leak-grep.sh + red-test PR proves it works |
| INFRA-05 | 01-05 | ✓ gitleaks pre-commit hook + history scan |
| INFRA-06 | 01-02 | ✓ pgcrypto + pg_cron + pg_net on staging |
| INFRA-07 | 01-02 | ✓ ops + stripe schemas on staging |
| INFRA-08 | 01-05 | ✓ npm audit clean |
| INFRA-09 | 01-04 + 01-07 | ✓ Sentry SPA + Edge instrumented; events verified |
| INFRA-10 | 01-06 | ✓ Twilio toll-free submitted |
| INFRA-11 | 01-06 | ✓ Stripe Canadian banking initiated |
| INFRA-12 | 01-06 | ✓ Resend DNS configured |
| INFRA-13 | 01-06 | ✓ CRA HST registered |
| SEC-07 | 01-03 + 01-07 | ✓ secret-grep + red-test prove service_role can't enter src/ |
| PLAT-07 | 01-04 + 01-07 | ✓ Sentry captures errors from both runtimes |

## What's deferred to later phases

These were intentionally NOT delivered in Phase 1 (per CONTEXT.md scope):
- Husky / lint-staged (Phase 7-8 if PR checks get slow)
- 1Password / shared vault (when 2nd contributor joins)
- Slack/Discord Sentry alerts (Phase 10)
- Backups + PITR (Phase 12)
- FreshFlow template code cleanup (Phase 12 Lighthouse)
- GitHub Projects board (if VENDORS.md becomes insufficient)

## Open items (vendor-side, awaiting external approval)

These do NOT block Phase 1 closure (per CONTEXT.md "definition of done = in flight"):
- Twilio toll-free: pending approval, ETA {date}, blocks Phase 5
- Stripe Canadian banking: pending {Plaid verified | micro-deposits in flight}, blocks Phase 6
- Resend domain: {Verified | DNS propagating}, blocks Phase 6
- CRA HST: registered (BN issued), no further action until Phase 4

## Phase closure

If all 5 criteria PASS and all 15 requirements ✓, Phase 1 is complete.
Update ROADMAP.md to mark "Phase 1: Foundation & Long-Lead Vendors" status complete and proceed to Phase 2 planning.
```

Fill in actual values (PR_URL, dates, event IDs, vendor statuses) by reading the prior plan SUMMARYs and evidence files.
  </action>
  <verify>
    <automated>powershell -Command "Test-Path .planning/phases/01-foundation-long-lead-vendors/PHASE-1-VERIFICATION.md; Select-String -Path .planning/phases/01-foundation-long-lead-vendors/PHASE-1-VERIFICATION.md -Pattern 'INFRA-01|INFRA-13|PLAT-07|SEC-07|RED-TEST-EVIDENCE|SENTRY-EVIDENCE|VENDORS' -SimpleMatch"</automated>
  </verify>
  <done>PHASE-1-VERIFICATION.md exists with all 5 success criteria mapped to evidence + all 15 requirements checked.</done>
</task>

</tasks>

<verification>
1. Red-test PR is open (`gh pr list --head test/secret-leak-red`) and the Secret Leak Grep check fails
2. test/secret-leak-red branch has restrict-deletion + restrict-force-push ruleset
3. Both Sentry event screenshots exist + event IDs in SENTRY-EVIDENCE.md
4. VENDORS.md has 4 non-"Not started" rows
5. PHASE-1-VERIFICATION.md ties all 5 ROADMAP success criteria to evidence
</verification>

<success_criteria>
- INFRA-04 + SEC-07: Red-test PR is permanent living evidence that the secret-grep works
- INFRA-09 + PLAT-07: Both Sentry projects show captured synthetic events
- Phase 1 closure: all 5 ROADMAP success criteria documented as PASS in PHASE-1-VERIFICATION.md
- Ready to proceed to Phase 2
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-long-lead-vendors/01-07-SUMMARY.md` summarizing: red-test PR URL, branch protection state, Sentry event IDs, and the final go/no-go for closing Phase 1.

After this plan, ROADMAP.md should be updated to mark Phase 1 complete (use Edit tool, change `- [ ] **Phase 1: ...` to `- [x] **Phase 1: ...` and update the Progress table row).
</output>
