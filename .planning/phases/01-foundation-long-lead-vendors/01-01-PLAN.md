---
phase: 01-foundation-long-lead-vendors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-foundation-long-lead-vendors/account-credentials.md
autonomous: false
requirements: [INFRA-01]
user_setup:
  - service: supabase
    why: "Two Supabase projects (staging + prod) — foundation for migrations, RLS, auth, Edge Functions"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens (personal access token, used by CLI in CI)"
      - name: STAGING_PROJECT_ID
        source: "Supabase Dashboard → hummi-staging → Settings → General → Reference ID"
      - name: PRODUCTION_PROJECT_ID
        source: "Supabase Dashboard → hummi-prod → Settings → General → Reference ID"
      - name: STAGING_DB_PASSWORD
        source: "Set during Supabase project creation; capture once, never recoverable from dashboard"
      - name: PRODUCTION_DB_PASSWORD
        source: "Set during Supabase project creation; capture once, never recoverable from dashboard"
    dashboard_config:
      - task: "Create project hummi-staging in us-east-1 (free tier)"
        location: "https://supabase.com/dashboard/new"
      - task: "Create project hummi-prod in us-east-1 (free tier)"
        location: "https://supabase.com/dashboard/new"
      - task: "Generate personal access token"
        location: "https://supabase.com/dashboard/account/tokens"
  - service: sentry
    why: "Error monitoring for SPA + Edge Functions (INFRA-09, PLAT-07)"
    env_vars:
      - name: VITE_SENTRY_DSN
        source: "Sentry → Settings → Projects → hummi-spa → Client Keys (DSN)"
      - name: SENTRY_DSN_EDGE
        source: "Sentry → Settings → Projects → hummi-edge → Client Keys (DSN)"
      - name: SENTRY_AUTH_TOKEN
        source: "Sentry → Settings → Account → API → Auth Tokens (scopes: project:releases, org:read)"
      - name: SENTRY_ORG
        source: "Sentry org slug (e.g., 'hummi')"
      - name: SENTRY_PROJECT_SPA
        source: "Sentry → Projects → hummi-spa (or whatever slug founder chose) — the project URL slug"
      - name: SENTRY_PROJECT_EDGE
        source: "Sentry → Projects → hummi-edge (or whatever slug founder chose) — the project URL slug"
    dashboard_config:
      - task: "Create Sentry org 'hummi' (or chosen slug)"
        location: "https://sentry.io/signup/"
      - task: "Create project 'hummi-spa' (platform: react)"
        location: "Sentry → Projects → New Project"
      - task: "Create project 'hummi-edge' (platform: deno)"
        location: "Sentry → Projects → New Project"
      - task: "Generate auth token with project:releases + org:read scopes"
        location: "Sentry → Settings → Auth Tokens"

must_haves:
  truths:
    - "Founder has created two Supabase projects (hummi-staging, hummi-prod) in us-east-1"
    - "Founder has created Sentry org + two projects (hummi-spa, hummi-edge or chosen slugs)"
    - "Both DB passwords are captured (one-time only — Supabase doesn't show them again)"
    - "Both Sentry DSNs, project slugs, and an auth token with release scopes are captured"
    - "All credentials are written to .planning/phases/01-foundation-long-lead-vendors/account-credentials.md (gitignored) for downstream plans to reference"
  artifacts:
    - path: ".planning/phases/01-foundation-long-lead-vendors/account-credentials.md"
      provides: "Captured project refs, DB passwords (placeholders), DSNs, project slugs, auth token — gitignored credential reference for downstream plans"
      contains: "STAGING_PROJECT_ID, PRODUCTION_PROJECT_ID, VITE_SENTRY_DSN, SENTRY_DSN_EDGE, SENTRY_ORG, SENTRY_PROJECT_SPA, SENTRY_PROJECT_EDGE, SENTRY_AUTH_TOKEN"
  key_links:
    - from: ".planning/phases/01-foundation-long-lead-vendors/account-credentials.md"
      to: ".gitignore"
      via: "must be gitignored — contains secrets"
      pattern: "account-credentials.md"
    - from: "Plans 02/03/04"
      to: ".planning/phases/01-foundation-long-lead-vendors/account-credentials.md"
      via: "downstream plans read project refs + DSNs + slugs from here before adding to GH Actions secrets and code"
      pattern: "account-credentials"
---

<objective>
Provision the external accounts that all other Phase 1 plans depend on: two Supabase projects (staging + prod) and a Sentry org with two projects (SPA + Edge). This is founder-owned manual work — Claude cannot create accounts via API. Capture all credentials (including Sentry project slugs, which 01-04's source-map upload needs) into a gitignored reference file so plans 02/03/04 can pick them up without re-prompting.

Purpose: Plans 02 (Supabase migration), 03 (CI workflows), and 04 (Sentry instrumentation) all need project IDs, DSNs, and Sentry project slugs that don't exist yet. This plan creates them and stages them for downstream consumption.
Output: Two Supabase projects, two Sentry projects, plus account-credentials.md with all the IDs/DSNs/slugs/tokens captured.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-long-lead-vendors/01-CONTEXT.md
@.planning/phases/01-foundation-long-lead-vendors/01-RESEARCH.md

Reference research sections:
- §"Standard Stack" — confirms `@sentry/react 10.53.1`, `@sentry/deno 10.53.1`, `supabase` CLI ≥ 2.84
- §"Open Questions" #2 + #4 — region us-east-1 for free tier, Sentry org slug to be chosen by founder
- §"Pattern 1: Supabase CLI for Two Projects" — explains why two separate projects, not branches
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare account-credentials.md placeholder + gitignore entry</name>
  <files>.planning/phases/01-foundation-long-lead-vendors/account-credentials.md, .gitignore</files>
  <action>
Create the credential capture file BEFORE founder action so the founder fills it in during the checkpoint.

1. Append to .gitignore (preserve existing entries):
   ```
   # Phase 1 captured credentials — never commit
   .planning/phases/*/account-credentials.md
   ```

2. Create .planning/phases/01-foundation-long-lead-vendors/account-credentials.md with this exact template (use Write tool, do NOT include real values):

   ```markdown
   # Phase 1 — Captured Account Credentials

   **STATUS: gitignored — do NOT commit. Lives only on founder's machine.**

   ## Supabase

   ### hummi-staging
   - Project Ref (STAGING_PROJECT_ID): __FILL_IN__
   - DB Password (STAGING_DB_PASSWORD): __FILL_IN__
   - Region: us-east-1
   - Created: __FILL_IN_DATE__

   ### hummi-prod
   - Project Ref (PRODUCTION_PROJECT_ID): __FILL_IN__
   - DB Password (PRODUCTION_DB_PASSWORD): __FILL_IN__
   - Region: us-east-1
   - Created: __FILL_IN_DATE__

   ### Supabase Access Token
   - SUPABASE_ACCESS_TOKEN: __FILL_IN__

   ## Sentry

   ### Org
   - SENTRY_ORG (slug): __FILL_IN__

   ### Project: hummi-spa (recommended slug — capture actual if founder chose differently)
   - VITE_SENTRY_DSN: __FILL_IN__
   - SENTRY_PROJECT_SPA (project URL slug — `hummi-spa` if recommended name kept): __FILL_IN__

   ### Project: hummi-edge (recommended slug — capture actual if founder chose differently)
   - SENTRY_DSN_EDGE: __FILL_IN__
   - SENTRY_PROJECT_EDGE (project URL slug — `hummi-edge` if recommended name kept): __FILL_IN__

   ### Auth Token (CI source-map upload)
   - SENTRY_AUTH_TOKEN: __FILL_IN__
   - Scopes: project:releases, org:read

   ---

   **Next step:** Plans 02, 03, 04 read this file and add the values to GitHub Actions secrets + Supabase Edge Function env vars. Once those are in place, this file can stay locally as a reference.

   **Why SENTRY_PROJECT_SPA matters:** 01-04's `vite.config.ts` uses `process.env.SENTRY_PROJECT_SPA || "hummi-spa"` for source-map upload. If the founder named the Sentry project something other than `hummi-spa`, CI source-map upload would silently 404 without this value set in GH Actions secrets.
   ```
  </action>
  <verify>
    <automated>powershell -Command "Test-Path .planning/phases/01-foundation-long-lead-vendors/account-credentials.md; Select-String -Path .gitignore -Pattern 'account-credentials.md' -SimpleMatch"</automated>
  </verify>
  <done>account-credentials.md exists with all __FILL_IN__ placeholders (including SENTRY_PROJECT_SPA and SENTRY_PROJECT_EDGE); .gitignore contains the pattern; git status confirms the file is ignored.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint 1: Founder creates Supabase + Sentry accounts and captures credentials</name>
  <action>Founder performs manual account creation in two SaaS dashboards (Supabase + Sentry). Claude cannot do this — requires login, 2FA, and payment-method-on-file decisions. The detailed step-by-step lives in how-to-verify below. Once founder completes, all credentials are pasted into the gitignored account-credentials.md and downstream plans (02, 03, 04) can pick them up.</action>
  <what-built>Credentials template file ready to receive founder's captured values.</what-built>
  <how-to-verify>
Founder performs these steps (Claude cannot — these require login + 2FA + payment-method-on-file decisions):

**1. Supabase (~15 min):**
   a. Go to https://supabase.com/dashboard and sign in
   b. Click "New project" → name: `hummi-staging`, region: `East US (North Virginia)` (us-east-1), free tier, set a strong DB password, copy it IMMEDIATELY into account-credentials.md (it's never shown again)
   c. Wait ~2 min for provisioning. Settings → General → copy "Reference ID" into STAGING_PROJECT_ID
   d. Repeat for `hummi-prod` (same region, different password)
   e. Account → Access Tokens → "Generate new token" → name: `hummi-ci`, copy into SUPABASE_ACCESS_TOKEN

**2. Sentry (~10 min):**
   a. Go to https://sentry.io/signup/ and create account (or sign in)
   b. Create org with slug `hummi` (or chosen slug); record in SENTRY_ORG
   c. Create project: platform `react`, name `hummi-spa` → copy DSN into VITE_SENTRY_DSN
      - **Capture the URL slug** of the project (visible at sentry.io/organizations/<org>/projects/<slug>/) → SENTRY_PROJECT_SPA. If you kept the recommended name, this is `hummi-spa`.
   d. Create second project: platform `Other` (Deno not listed; pick `Other` and set name `hummi-edge`) → copy DSN into SENTRY_DSN_EDGE
      - **Capture the URL slug** → SENTRY_PROJECT_EDGE. If you kept the recommended name, this is `hummi-edge`.
   e. Settings → Auth Tokens → "Create New Token" → scopes: `project:releases`, `org:read` → copy into SENTRY_AUTH_TOKEN

**3. Save all values into account-credentials.md (replace every __FILL_IN__).**

**4. Verify gitignore is working:**
   ```powershell
   git check-ignore .planning/phases/01-foundation-long-lead-vendors/account-credentials.md
   ```
   Should print the path (= ignored). If it prints nothing, the file is NOT ignored — STOP and fix .gitignore before continuing.
  </how-to-verify>
  <resume-signal>Type "done" once account-credentials.md has all real values filled in (including SENTRY_PROJECT_SPA and SENTRY_PROJECT_EDGE) AND `git check-ignore` confirms the file is ignored. If a step blocked you (e.g., Supabase free-tier limit, Sentry signup issue), describe it instead.</resume-signal>
</task>

</tasks>

<verification>
After this plan:
1. Run `git check-ignore .planning/phases/01-foundation-long-lead-vendors/account-credentials.md` — must print the path
2. Open account-credentials.md and grep for `__FILL_IN__` — must return zero matches
3. Verify two Supabase projects exist by visiting https://supabase.com/dashboard
4. Verify Sentry org + two projects exist by visiting https://sentry.io
5. Confirm SENTRY_PROJECT_SPA and SENTRY_PROJECT_EDGE values match the actual project URL slugs (matters for 01-04 source-map upload)
</verification>

<success_criteria>
- account-credentials.md has zero __FILL_IN__ placeholders remaining
- File is confirmed gitignored
- Both Supabase project refs are captured and verifiable in dashboard
- Both Sentry DSNs and project slugs are captured and verifiable in dashboard
- Plans 02/03/04 can begin without further founder prompting
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-long-lead-vendors/01-01-SUMMARY.md` summarizing: which accounts were created, the redacted shape of credentials captured (e.g., "VITE_SENTRY_DSN: https://[redacted]@o[redacted].ingest.sentry.io/[redacted]"), the actual Sentry project slugs captured (defaults likely `hummi-spa` / `hummi-edge`), and any setup gotchas hit.
</output>
